import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    headingWrap: {
        padding: 30,
        justifyContent: 'flex-end',
        height: '50%',
        width: '100%'
    },
    heading: {
        fontSize: 55,
        fontWeight: "bold",
        color: 'white'
    }
});

export { Styles }